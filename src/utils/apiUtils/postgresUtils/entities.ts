import { Guild } from "../../../entities/Guild";
import { GuildActivity } from "../../../entities/GuildActivity";
import { GuildChannel } from "../../../entities/GuildChannel";
import { GuildCurrency } from "../../../entities/GuildCurrency";
import { GuildRemovalReason } from "../../../entities/GuildRemovalReason";
import { GuildRole } from "../../../entities/GuildRole";

export const entities = [
  Guild,
  GuildActivity,
  GuildCurrency,
  GuildRemovalReason,
  GuildChannel,
  GuildRole,
];
